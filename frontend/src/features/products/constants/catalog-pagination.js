/** Customer catalog grid page size (matches existing products page pagination). */
export const PRODUCTS_PAGE_SIZE = 12;

/** Max products fetched per API request (matches backend MAX_LIMIT). */
export const PRODUCTS_CATALOG_FETCH_LIMIT = 100;
